---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';
import { MorphingBlob } from '@/components/ui/MorphingBlob';
import { CustomCursor } from '@/components/ui/CustomCursor';
import FAQSection from '@/components/sections/FAQSection';

const title = 'Desarrollo de Software a Medida en Colombia | Simio CodeLabs';
const description = 'Software que funciona. Automatización, e-commerce y sistemas a medida. 5+ años resolviendo problemas reales.';

const portfolioItems = await getCollection('portfolio', ({ data }) => {
  return data.lang === 'es';
});

const projectItems = await getCollection('projects', ({ data }) => {
  return data.lang === 'es' && data.featured;
});

const featuredProjects = portfolioItems.slice(0, 3);

const featuredProjectsList = projectItems
  .sort((a, b) => a.data.order - b.data.order)
  .slice(0, 4);

const faqs = [
  {
    question: '¿Cuánto cuesta un proyecto?',
    answer: 'Depende totalmente del alcance. Un script de automatización simple puede costar desde $500, mientras que un e-commerce completo empieza en $3,000. Te doy presupuesto fijo después de una llamada gratuita donde entiendo qué necesitas. Sin sorpresas, sin costos ocultos.',
  },
  {
    question: '¿Cuánto tiempo toma?',
    answer: 'Un sistema de automatización básico: 1-2 semanas. Un e-commerce funcional: 3-4 semanas. Un ERP personalizado: 6-8 semanas. Trabajo en sprints semanales, así que ves avances cada 7 días. Si alguien te promete algo complejo en días, huye.',
  },
  {
    question: '¿Solo trabajas con empresas grandes?',
    answer: 'No. Principalmente trabajo con PYMEs y startups. No necesitas ser Google para tener buen software. Si tienes un problema real que resolver y presupuesto para solucionarlo, podemos trabajar juntos. Lo que NO hago: páginas baratas ni trabajo "por exposición".',
  },
  {
    question: '¿Das soporte después de entregar?',
    answer: 'Sí. Incluyo 30 días de soporte post-lanzamiento gratis (bugs, ajustes menores). Después puedes contratar soporte mensual o por horas. También te entrego documentación y código fuente completo, no te dejo atrapado.',
  },
  {
    question: '¿Por qué debería contratarte en vez de usar una plantilla?',
    answer: 'Las plantillas son geniales... si tu negocio es genérico. Si necesitas algo específico (integrar con tu sistema actual, automatizar TU proceso, no "un proceso"), necesitas código a medida. Además, con plantillas terminas pagando subscripciones eternas. Yo te vendo el software, tú lo posees.',
  },
  {
    question: '¿Haces reuniones presenciales?',
    answer: 'Trabajo 100% remoto. Tenemos calls por Google Meet cuando sea necesario, pero todo el trabajo es online. Así puedo trabajar con clientes en toda Colombia (y Latam) sin limitarme a una ciudad. Más flexibilidad, menos pérdida de tiempo en tráfico.',
  },
];
---

<BaseLayout title={title} description={description} lang="es">
  <CustomCursor client:media="(min-width: 1024px)" />

  <div class="min-h-screen bg-background gradient-mesh">
    <section class="relative min-h-screen flex items-center justify-center px-4 sm:px-6 py-16 sm:py-20 overflow-hidden">
      <MorphingBlob client:visible className="hidden lg:block top-0 left-0 opacity-20" color="#A78BFA" size={500} />

      <div class="relative z-10 mx-auto max-w-7xl w-full">
        <div class="space-y-12 max-w-3xl">
          <h1 class="text-4xl sm:text-5xl md:text-6xl lg:text-7xl xl:text-8xl font-black tracking-tighter leading-[1.1]">
            <span class="block text-foreground">Desarrollo de Software a Medida</span>
            <span class="block bg-gradient-to-r from-primary via-purple-500 to-primary bg-clip-text text-transparent">
              Automatización
            </span>
            <span class="block text-foreground">y Sistemas Web en Colombia</span>
          </h1>

          <p class="max-w-2xl text-xl md:text-2xl text-muted-foreground leading-relaxed">
            Desarrollo software que funciona. Sin plantillas genéricas, sin subscripciones eternas. Solo código que <span class="text-foreground font-semibold">resuelve tu problema específico</span>.
          </p>

          <div class="flex flex-col sm:flex-row items-start gap-4">
            <a
              href="#contacto"
              class="group relative inline-flex items-center justify-center gap-2 rounded-full bg-primary px-8 py-4 text-lg font-semibold text-primary-foreground transition-all hover:scale-105 hover:shadow-2xl hover:shadow-primary/50"
              title="Agendar consulta gratuita de 30 minutos"
            >
              Hablemos (es gratis)
              <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
              </svg>
            </a>
            <a
              href="/portafolio/"
              class="inline-flex items-center justify-center gap-2 rounded-full border-2 border-border bg-background px-8 py-4 text-lg font-semibold transition-all hover:border-primary hover:bg-muted"
              title="Ver casos de éxito y proyectos completados"
            >
              Ver qué he hecho
            </a>
          </div>

          <div class="grid grid-cols-3 gap-8 max-w-2xl pt-8">
            {[
              { value: '5+', label: 'Años' },
              { value: '20+', label: 'Proyectos' },
              { value: '90%', label: 'Eficiencia' }
            ].map((stat) => (
              <div class="text-center">
                <div class="text-4xl md:text-5xl font-black text-primary">{stat.value}</div>
                <div class="text-sm text-muted-foreground mt-2">{stat.label}</div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </section>

    <section class="relative px-6 py-32 lg:px-8">
      <div class="mx-auto max-w-7xl">
        <div class="mb-12 md:mb-16">
          <h2 class="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-black tracking-tighter mb-4 md:mb-6">
            Servicios
          </h2>
          <p class="text-lg md:text-xl text-muted-foreground max-w-2xl">
            Soluciones diseñadas para hacer crecer tu negocio
          </p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-5">
          <div class="group rounded-2xl border border-border bg-card p-6 transition-all hover:border-primary/50 hover:shadow-lg">
            <div class="mb-4 text-primary">
              <svg class="w-10 h-10" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" />
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
              </svg>
            </div>
            <h3 class="text-xl font-semibold mb-3 group-hover:text-primary transition-colors">
              Automatización Inteligente
            </h3>
            <p class="text-muted-foreground text-sm leading-relaxed mb-4">
              Elimina tareas repetitivas y libera tiempo de tu equipo para actividades estratégicas de alto valor.
            </p>
            <div class="flex flex-wrap gap-2">
              <span class="inline-flex items-center rounded-md bg-primary/10 px-2.5 py-1 text-xs font-medium text-primary">Python</span>
              <span class="inline-flex items-center rounded-md bg-primary/10 px-2.5 py-1 text-xs font-medium text-primary">Node.js</span>
              <span class="inline-flex items-center rounded-md bg-primary/10 px-2.5 py-1 text-xs font-medium text-primary">APIs</span>
            </div>
          </div>

          <div class="group rounded-2xl border border-border bg-card p-6 transition-all hover:border-primary/50 hover:shadow-lg">
            <div class="mb-4 text-primary">
              <svg class="w-10 h-10" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z" />
              </svg>
            </div>
            <h3 class="text-xl font-semibold mb-3 group-hover:text-primary transition-colors">
              Tiendas Online
            </h3>
            <p class="text-muted-foreground text-sm leading-relaxed">
              E-commerce optimizado para máxima conversión. Desde catálogos simples hasta plataformas complejas con inventario, pagos y envíos.
            </p>
          </div>

          <div class="group rounded-2xl border border-border bg-card p-6 transition-all hover:border-primary/50 hover:shadow-lg">
            <div class="mb-4 text-primary">
              <svg class="w-10 h-10" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4 7v10c0 2.21 3.582 4 8 4s8-1.79 8-4V7M4 7c0 2.21 3.582 4 8 4s8-1.79 8-4M4 7c0-2.21 3.582-4 8-4s8 1.79 8 4m0 5c0 2.21-3.582 4-8 4s-8-1.79-8-4" />
              </svg>
            </div>
            <h3 class="text-xl font-semibold mb-3 group-hover:text-primary transition-colors">
              Software de Gestión
            </h3>
            <p class="text-muted-foreground text-sm leading-relaxed">
              ERPs y CRMs adaptados a tu flujo de trabajo. Gestiona clientes, inventarios, ventas y finanzas en un solo lugar.
            </p>
          </div>

          <div class="group rounded-2xl border border-border bg-card p-6 transition-all hover:border-primary/50 hover:shadow-lg">
            <div class="mb-4 text-primary">
              <svg class="w-10 h-10" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M13 10V3L4 14h7v7l9-11h-7z" />
              </svg>
            </div>
            <h3 class="text-xl font-semibold mb-3 group-hover:text-primary transition-colors">
              Landing Pages de Alta Conversión
            </h3>
            <p class="text-muted-foreground text-sm leading-relaxed mb-4">
              Páginas diseñadas específicamente para convertir visitantes en clientes. Diseño persuasivo y analítica integrada.
            </p>
            <div class="flex gap-3 pt-4 border-t border-border">
              <div class="flex-1 rounded-xl bg-muted p-3">
                <div class="text-xs text-muted-foreground mb-1">Antes</div>
                <div class="text-sm font-bold">1.5% conversión</div>
              </div>
              <div class="flex-1 rounded-xl bg-primary/10 p-3">
                <div class="text-xs text-primary mb-1">Después</div>
                <div class="text-sm font-bold text-primary">4.5% conversión</div>
              </div>
            </div>
          </div>
        </div>

        <div class="mt-12 text-center">
          <a
            href="/servicios/"
            class="inline-flex items-center gap-2 text-lg font-semibold text-primary hover:text-primary/80 transition-colors group"
            title="Conocer todos los servicios de desarrollo de software a medida"
          >
            Ver todos los servicios
            <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
            </svg>
          </a>
        </div>
      </div>
    </section>

    {featuredProjects.length > 0 && (
      <section class="relative px-6 py-32 lg:px-8 bg-muted/30">
        <div class="mx-auto max-w-7xl">
          <div class="mb-12 md:mb-16">
            <h2 class="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-black tracking-tighter mb-4 md:mb-6">
              Casos de Éxito
            </h2>
            <p class="text-lg md:text-xl text-muted-foreground max-w-2xl">
              Resultados reales de proyectos completados
            </p>
          </div>

          <div class="grid grid-cols-1 md:grid-cols-2 gap-5">
            {featuredProjects.slice(0, 2).map((project) => (
              <a
                href={`/portafolio/${project.slug.replace(/^(es|en)\//, '')}/`}
                class="group relative rounded-2xl border border-border bg-card overflow-hidden transition-all hover:border-primary/50 hover:shadow-lg"
                title={`Ver caso de éxito: ${project.data.title}`}
              >
                <div class="aspect-[16/9] overflow-hidden bg-muted relative">
                  {project.data.image ? (
                    <img
                      src={project.data.image}
                      alt={project.data.title}
                      class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
                      loading="lazy"
                      decoding="async"
                      width="800"
                      height="450"
                    />
                  ) : (
                    <div class="w-full h-full bg-gradient-to-br from-primary/20 to-purple-500/20"></div>
                  )}
                  <div class="absolute inset-0 bg-gradient-to-t from-card/90 via-card/20 to-transparent"></div>
                </div>
                <div class="p-5">
                  <h3 class="text-lg font-semibold mb-2 group-hover:text-primary transition-colors">
                    {project.data.title}
                  </h3>
                  <p class="text-muted-foreground text-sm mb-3 leading-relaxed line-clamp-2">
                    {project.data.description}
                  </p>
                  {project.data.technologies && (
                    <div class="flex flex-wrap gap-1.5 mb-3">
                      {project.data.technologies.slice(0, 3).map((tech) => (
                        <span class="inline-flex items-center rounded-md bg-primary/10 px-2 py-0.5 text-xs font-medium text-primary">
                          {tech}
                        </span>
                      ))}
                    </div>
                  )}
                  {project.data.metrics && (
                    <div class="inline-flex items-center gap-2 rounded-full bg-primary/10 px-3 py-1.5 text-xs font-semibold text-primary">
                      <svg class="w-3 h-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6" />
                      </svg>
                      {project.data.metrics.improvement}
                    </div>
                  )}
                </div>
              </a>
            ))}
          </div>
        </div>
      </section>
    )}

    {featuredProjectsList.length > 0 && (
      <section id="proyectos" class="relative px-6 py-32 lg:px-8">
        <div class="mx-auto max-w-7xl">
          <div class="mb-12 md:mb-16">
            <h2 class="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-black tracking-tighter mb-4 md:mb-6">
              Proyectos Destacados
            </h2>
            <p class="text-lg md:text-xl text-muted-foreground max-w-2xl">
              Soluciones innovadoras creadas a medida con enfoque en resultados
            </p>
          </div>

          <div class="grid grid-cols-1 md:grid-cols-2 gap-5">
            {featuredProjectsList.map((project) => (
              <article class="group relative rounded-2xl border border-border bg-card overflow-hidden transition-all hover:border-primary/50 hover:shadow-lg">
                <div class="aspect-[16/9] overflow-hidden bg-muted relative">
                  <img
                    src={project.data.image}
                    alt={project.data.title}
                    class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
                    loading="lazy"
                    decoding="async"
                    width="800"
                    height="450"
                  />
                  <div class="absolute inset-0 bg-gradient-to-t from-card/90 via-card/20 to-transparent"></div>
                </div>
                <div class="p-5">
                  <h3 class="text-lg font-semibold mb-2 group-hover:text-primary transition-colors">
                    {project.data.title}
                  </h3>
                  <p class="text-muted-foreground text-sm mb-3 leading-relaxed line-clamp-2">
                    {project.data.description}
                  </p>
                  <div class="flex flex-wrap gap-1.5">
                    {project.data.technologies.slice(0, 3).map((tech) => (
                      <span class="inline-flex items-center rounded-md bg-primary/10 px-2 py-0.5 text-xs font-medium text-primary">
                        {tech}
                      </span>
                    ))}
                  </div>
                </div>
              </article>
            ))}
          </div>
        </div>
      </section>
    )}

    <section class="relative px-6 py-16 md:py-24 lg:px-8 bg-muted/30">
      <div class="mx-auto max-w-6xl">
        <div class="text-center mb-12 md:mb-16">
          <h2 class="text-4xl sm:text-5xl md:text-6xl font-black tracking-tighter mb-4">
            Por qué no soy tu típico freelancer
          </h2>
          <p class="text-lg md:text-xl text-muted-foreground max-w-2xl mx-auto">
            Sin promesas exageradas. Solo trabajo honesto y código que funciona.
          </p>
        </div>

        <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
          <div class="rounded-2xl border border-border bg-card p-6">
            <div class="text-4xl mb-4">🎯</div>
            <h3 class="text-xl font-bold mb-3 text-foreground">Enfoque en resultados</h3>
            <p class="text-muted-foreground">
              No me importa usar la última tecnología de moda si no resuelve tu problema. Uso lo que funciona: React cuando necesitas interactividad, Python para automatización, lo que sea necesario para que TU negocio crezca.
            </p>
          </div>

          <div class="rounded-2xl border border-border bg-card p-6">
            <div class="text-4xl mb-4">💬</div>
            <h3 class="text-xl font-bold mb-3 text-foreground">Comunicación clara</h3>
            <p class="text-muted-foreground">
              Cero jerga innecesaria. Te explico qué estoy haciendo, por qué lo hago así, y cuánto va a costar. Updates semanales, acceso al repositorio, sin secretos. Tu proyecto, tu código.
            </p>
          </div>

          <div class="rounded-2xl border border-border bg-card p-6">
            <div class="text-4xl mb-4">⚡</div>
            <h3 class="text-xl font-bold mb-3 text-foreground">Entrega iterativa</h3>
            <p class="text-muted-foreground">
              No esperas 3 meses para ver algo. Trabajo en sprints de 1 semana. Cada viernes ves progreso real. Algo no te gusta? Lo ajustamos la próxima semana. Así evitamos sorpresas al final.
            </p>
          </div>

          <div class="rounded-2xl border border-border bg-card p-6">
            <div class="text-4xl mb-4">📚</div>
            <h3 class="text-xl font-bold mb-3 text-foreground">Documentación incluida</h3>
            <p class="text-muted-foreground">
              Todos mis proyectos incluyen documentación. No te dejo con un sistema que solo yo entiendo. Si mañana necesitas otro dev, puede entender qué hice y cómo funciona todo.
            </p>
          </div>

          <div class="rounded-2xl border border-border bg-card p-6">
            <div class="text-4xl mb-4">🛡️</div>
            <h3 class="text-xl font-bold mb-3 text-foreground">Sin vendor lock-in</h3>
            <p class="text-muted-foreground">
              Tu código es tuyo. No uso plataformas propietarias que te aten a subscripciones eternas. Si decides irte con otro dev o llevarlo in-house, puedes. Detesto los rehenes digitales.
            </p>
          </div>

          <div class="rounded-2xl border border-border bg-card p-6">
            <div class="text-4xl mb-4">🔧</div>
            <h3 class="text-xl font-bold mb-3 text-foreground">Mantenimiento real</h3>
            <p class="text-muted-foreground">
              30 días de soporte gratis post-lanzamiento. Después, opciones flexibles: soporte mensual, por horas, o nada si prefieres manejarlo tú. No te obligo a pagar mantenimiento que no necesitas.
            </p>
          </div>
        </div>
      </div>
    </section>

    <FAQSection client:visible faqs={faqs} lang="es" />

    <section id="contacto" class="relative px-4 sm:px-6 py-16 md:py-32 lg:px-8">
      <div class="mx-auto max-w-4xl">
        <div class="relative rounded-2xl md:rounded-3xl border-2 border-primary/20 bg-card p-6 sm:p-8 md:p-12 text-center overflow-hidden">
          <MorphingBlob client:visible className="hidden sm:block top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 opacity-20" color="#818CF8" size={400} />

          <div class="relative z-10">
            <h2 class="text-2xl sm:text-3xl md:text-4xl lg:text-5xl font-black tracking-tighter mb-4 md:mb-6">
              ¿Listo para <span class="text-primary">transformar</span> tu negocio?
            </h2>
            <p class="text-base sm:text-lg md:text-xl text-muted-foreground mb-6 sm:mb-8 md:mb-10 max-w-2xl mx-auto">
              Agenda un diagnóstico gratuito de 30 minutos. Sin compromiso, sin costo.
            </p>
            <a
              href="mailto:nestcdev@gmail.com"
              class="inline-flex items-center justify-center gap-2 sm:gap-3 rounded-full bg-primary px-5 sm:px-6 md:px-8 py-3 sm:py-3.5 md:py-4 text-sm sm:text-base md:text-lg font-semibold text-primary-foreground shadow-2xl transition-all hover:scale-105 hover:shadow-primary/50 w-full sm:w-auto max-w-full"
              title="Enviar correo para agendar consulta gratuita"
            >
              <svg class="w-4 h-4 sm:w-5 sm:h-5 md:w-6 md:h-6 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
              </svg>
              <span class="truncate">nestcdev@gmail.com</span>
            </a>
          </div>
        </div>
      </div>
    </section>
  </div>
</BaseLayout>
